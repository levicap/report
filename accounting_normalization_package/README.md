# Package contents

Start with `accounting_normalization_audit.md` for the findings and decisions.

Use `accounting_report.schema.json` as the JSON contract.

Use `vendor_mapping_config.json` as the parser and posting policy draft.

Use `field_mapping_matrix.csv` for source field mapping.

Use `file_manifest.csv` and `report_group_audit.csv` for file and bundle review.

The schema examples were checked in `schema_validation_results.csv`.
